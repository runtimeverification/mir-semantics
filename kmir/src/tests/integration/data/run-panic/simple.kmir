#init(0,
0 |-> FnVal(
  basicBlock(
    emptyBlock,
    terminator(terminatorKindCall(
    operandConstant(constOperand(span(0), noUserTypeAnnotationIndex, mirConst(constantKindZeroSized, ty(0, tyKindRigidTy( rigidTyFnDef( 1, emptyArgs ))), mirConstId(0)))),
    emptyOperands,
    place(local(1), noProjection),
    noBasicBlockIdx,
    unwindActionContinue), span(2))),
  2,
  0)
1 |-> FnVal(
  basicBlock(emptyBlock, terminator(terminatorKindUnreachable, span(3))),
  0,
  0)
)
