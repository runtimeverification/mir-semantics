#init(0,
0 |-> ListItem( body(
        basicBlock(
          emptyBlock,
          terminator(terminatorKindCall(
          operandConstant(constOperand(span(0), noUserTypeAnnotationIndex, mirConst(constantKindFnDef(1), ty(1), mirConstId(0)))),
          emptyOperands,
          place(local(0), noProjection),
          someBasicBlockIdx( basicBlockIdx( 1 ) ),
          unwindActionContinue), span(2))
        )
        basicBlock(
          emptyBlock,
          terminator(terminatorKindReturn, span(2))
        )
        ,
        localDecl(ty(0), span(0), mutabilityMut),
        0,
        noVarDebug,
        noLocal,
        span(0)
      ) )
1 |-> ListItem( body(
        basicBlock(
          emptyBlock,
          terminator(terminatorKindUnreachable, span(3))),
        localDecl(ty(0), span(0), mutabilityMut),
        0,
        noVarDebug,
        noLocal,
        span(0)
      ) ),
emptyMap
)
